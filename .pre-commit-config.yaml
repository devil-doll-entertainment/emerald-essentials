repos:
  - repo: local
    hooks:
      - id: just-check
        name: Quality Gate (just check)
        entry: just check
        language: system
        pass_filenames: false
        always_run: true
